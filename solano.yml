---
:timeout: 300
hooks:
  post_worker: "cp test/reports/*.xml $HOME/results/$TDDIUM_SESSION_ID/session/; cp /mnt/home/u3/log/task*.log $HOME/results/$TDDIUM_SESSION_ID/session/ 2>/dev/null || :"

:ruby_version: 'ruby-2.1.2'

#:test_pattern:
#- features/*.feature
#- spec/**_spec.rb

:tests:
- type: ruby-testunit
  mode: basic
  report_files:
  - "test/reports/TEST-*.xml"
